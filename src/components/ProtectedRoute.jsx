// Since we want to allow all users to view the DisplayPage, we don't need the ProtectedRoute component anymore.
// If there were specific routes that still needed protection, we could reintroduce this component with the necessary logic.
